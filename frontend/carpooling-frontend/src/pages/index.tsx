import { Typography } from '@mui/material';

export default function Home() {
  return (
    <div>
      <Typography variant="h4">Welcome to Carpooling System</Typography>
      <Typography>Share rides, save costs, and reduce emissions!</Typography>
    </div>
  );
}